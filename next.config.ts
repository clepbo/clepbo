import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The desk is one page of hand-written CSS; nothing to transpile beyond React.
  reactStrictMode: true,
};

export default nextConfig;
